<Window x:Class="Hospital.View.Admin.Pages.Analytics.AnalyticsHospitalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View.Admin.Pages.Analytics"
        mc:Ignorable="d"
        Title="AnalyticsHospitalView" Height="450" Width="800">
    <Grid>
        <DataGrid Name="DG1" ItemsSource="{Binding}" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Stavka"  Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Prosecna Ocena" Binding="{Binding LastName}" Width="*" />
                <DataGridHyperlinkColumn Header="Broj Ocena" Binding="{Binding Email}" Width="*"  />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
